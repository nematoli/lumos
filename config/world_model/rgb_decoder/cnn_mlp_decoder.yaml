_target_: lumos.world_models.decoders.cnn_mlp_decoder.CnnMLPDecoder
in_dim: ???
cnn_depth: ${world_model.encoder.cnn_depth}
kernels: [5, 5, 6, 6]
stride: 2
layer_norm: true
activation: elu
mlp_layers: 0
use_gripper_camera: ${world_model.use_gripper_camera}
robot_dim: ${world_model.encoder.robot_dim}
scene_dim: ${world_model.encoder.scene_dim}
state_mlp_layers: [400]
